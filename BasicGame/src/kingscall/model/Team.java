package kingscall.model;

/**
 * The two partnerships. {@link #HUMAN} is the side the human player sits on
 * ("you" on the scoreboard); {@link #OPPONENT} is the other pair.
 */
public enum Team {
    HUMAN,
    OPPONENT
}
